#include <unordered_set>
#include <iostream>
using namespace std;

int main(){
    // everything is the same as set
    // only that unordered set stores elements in unordered manner

    unordered_set<int> s;
}